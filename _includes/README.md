# `_includes`

Liquid partials (fragments) included by layouts and pages — e.g. `author-profile.html`
(the sidebar) and `archive-single.html` (the list-item card). This is part of the
**theme layer**: changes here affect every page on the site, so you rarely touch these
unless you are customizing the theme.