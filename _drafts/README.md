# `_drafts`

Jekyll drafts — blog posts that are not yet published. Drafts do **not** appear on the
site unless you build/serve with `--drafts`:

```bash
bundle exec jekyll serve --drafts
```

Draft filenames do not need a date prefix. To publish a draft, move it into `_posts/`
and rename it to `YYYY-MM-DD-title.md`.